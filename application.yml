#  Spring Boot 기본 설정 및 DB 연동 (AWS EC2 MariaDB)
spring:
  datasource:
    url: jdbc:mariadb://15.135.113.196:3306/a5_db?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: a5_admin
    password: 4622
    driver-class-name: org.mariadb.jdbc.Driver

  #  중복 선언된 spring 계층을 제거하고 올바른 위치로 수정 -- 현재 구글 메일 안 보내짐 추후 다른 메일이나 다른 방식 고밍(로그랑 메시지는 나옴)
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${"메일주소"}
    password: ${"메일비번"}
    
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true

#  서버 구동 포트
server:
  port: 8080

#  AI 연동 설정
ai:
  gemini:
    url: https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent
    key: ${"제미나이 키"}
  chatgpt:
    url: "https://api.openai.com/v1/chat/completions"
    key: "YOUR_CHATGPT_API_KEY"
  api:
    mock: true

#  Navitime API 설정 (RapidAPI)
navitime:
  api:
    key: ${"navitime 키"}

#  Google Maps API 설정
google:
  maps:
    key: ${"Google Maps API 키"}

#  디버그 모드 활성화
debug: true
